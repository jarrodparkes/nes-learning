# Input

In this example, we will detect inputs from a controller.

## Instructions

```bash
# build example
nesasm tiles.asm

# run example
Nestopia tiles.nes

# build and run example
./build-and-run.sh
```
